apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot
  template:
    metadata:
      labels:
        app: bot
    spec:
      containers:
      - name: bot
        image: ghcr.io/python-discord/bot:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 25m
            memory: 500Mi
          limits:
            cpu: 200m
            memory: 750Mi
        envFrom:
        - secretRef:
            name: bot-env
        - secretRef:
            name: sentry-env
