apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: metabase
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`metabase.pythondiscord.com`)
    kind: Rule
    services:
    - name: metabase
      port: 80
    middlewares:
    - name: global-chain
  tls:
    certResolver: cfresolver
    domains:
    - main: "*.pythondiscord.com"
